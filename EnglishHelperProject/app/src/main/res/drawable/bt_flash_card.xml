<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Hình tròn màu trắng -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#FFFFFF"/>
        </shape>
    </item>

    <!-- Hình ảnh nhỏ lại -->
    <item
        android:gravity="center"
        android:width="30dp"
        android:height="30dp"
        android:drawable="@drawable/flash_card"/>
</layer-list>
